services:
  - type: web
    name: umdatax-backend
    env: python
    buildCommand: pip install -r backend/requeriments.txt && playwright install chromium && playwright install-deps chromium
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port 10000